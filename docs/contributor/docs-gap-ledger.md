# Docs-gap ledger

> **Audience:** contributor
> **Status:** current (working file)
> **Verified against:** n/a
> **Canonical for:** software gaps discovered while documenting; each row becomes a `docs-gap` issue in the owning repo at the end of each phase

**Phase 0+1 done 2026-08-23; rows 1–32 are ready to file as issues (Phase 4). Phase 2 is appending from row 33.**

| # | Repo | Gap (what the doc wanted to say) | What is true today | Page that needs it | Issue |
|---|------|----------------------------------|--------------------|--------------------|-------|
| 1 | sigmond | one command that reports attached hardware (RX888 serial, GPSDO model, mag sensor) | scattered: `smd admin environment`, `smd watch gpsdo`, mag logs | operator/day-2.md, hardware/shopping-list.md | — |
| 2 | sigmond-appliance | the RAC dashboard's generated ssh command should work as printed | it hands out `ssh -p <vm-ssh-port> root@<gateway>` (observed on the dashboard 2026-08-17), but every station's decoder VM ships `PermitRootLogin no` in `/etc/ssh/sshd_config.d/10-sigmond-operator.conf` (re-verified live on b4 AND dasi002, 2026-08-23), so the port answers, the host key exchanges and auth always fails — indistinguishable from a broken tunnel. The working forms are `hamsci@`/`sigmond@` for the VM channel and `root@` for the host-ssh channel only | operator/remote-access.md | — |
| 3a | hs-uploader | PSWS transport for a new client's products | only hf-timestd/GRAPE + mag ship to PSWS; a new client has no documented PSWS path | scientist/becoming-a-client.md (Phase 2) | — |
| 3b | hs-uploader | GRAPE uploader should honor the same `[uploads]` policy that gates other clients' PSWS shipping | the GRAPE uploader ships regardless of `[uploads]` settings in `coordination.toml`, unlike wspr/psk/mag, which `smd config uploads status` can pause | operator/registration.md §6, scientist/becoming-a-client.md (Phase 2) | — |
| 4 | sigmond | client scaffold command (`smd component add` takes a repo, not a template) | ADD-A-CLIENT says "copy psk-recorder" | scientist/becoming-a-client.md (Phase 2) | — |
| 5 | sigmond | which GPSDO output is patched into which jack (RX888 reference, TS-1 reference) | nothing on the station records the physical cabling; `gpsdo-monitor` reports OUT1/OUT2 frequencies (b4: 10 MHz / 27 MHz) but not where they go | hardware/shopping-list.md | — |
| 6 | sigmond | `smd status` is the one command the operator front page tells a novice to run, so its output should be self-explanatory | on b4 it prints `sudo: a password is required` twice (stderr) before the status banner — a read-only verb attempting sudo; an operator cannot tell that from a real failure (source: live b4, 2026-08-23; not seen on dasi002) | operator/README.md, operator/day-2.md | — |
| 7 | sigmond | "your PSWS enrolment is finished" should be one answer | two key models coexist and disagree in the operator's face: `smd psws status` reports the station-key model (b4: `✓ key verified 2026-08-17T22:33:29Z`) while `smd status` simultaneously prints `━━━ PSWS upload not finished ━━━` for the legacy per-recorder keys (`/home/timestd/.ssh/id_rsa_psws`, `/etc/mag-recorder/keys/id_ed25519`) that a station-key host does not use (source: live b4, 2026-08-23; `lib/sigmond/psws.py` `installed_unconfigured()` vs `cmd_site_status()`) | operator/registration.md §5c, operator/troubleshooting.md | — |
| 8 | sigmond | one command that answers "are my uploads being accepted?" across all four paths | `smd watch uploads` is a live tail (wsprnet/wsprdaemon/pskreporter only, and only while something ships); PSWS acceptance is only visible on the portal — an operator has to combine a tail, two web pages and a portal login. Worse, nobody on the project has a PSWS account documented well enough to say what the portal's data view looks like or how to reach a site's files, so the operator page cannot even name the page to look at | operator/registration.md §6 | — |
| 9 | hf-timestd + sigmond | one PSWS portal address | THREE are in circulation and nothing reconciles them: `lib/sigmond/psws.py` (`PSWS_PORTAL`), `scripts/proxmox/sigmond-wizard.sh:463` and `smd psws enroll` say `https://pswsnetwork.eng.ua.edu/`; `hf-timestd/docs/PSWS_SETUP_GUIDE.md:20-21` presents that as the *Server URL* and names `pswsnetwork.caps.ua.edu` as the separate *Registration Portal* (a current distinction, not a stale page), matched by `sigmond/docs/PROVISIONING-INPUTS.md:61`; `hf-timestd/docs/STATION_SETUP_GUIDE.md:88` gives a third, `pswsnetwork.org`. The operator page has to hedge across all three | operator/registration.md §5a | — |
| 10 | hf-timestd | one PSWS enrolment procedure | `hf-timestd/docs/PSWS_SETUP_GUIDE.md` steps 4-6 document the OLDER per-recorder model (a dedicated RSA key per recorder, a portal TOKEN, `ssh-copy-id`, `hf-timestd grape test-upload`); appliance stations use one station key (`/etc/hs-uploader/keys/id_ed25519_host`) with `smd psws enroll` / `smd psws verify` (`lib/sigmond/psws.py`, site-level enrolment). Neither document says the other exists, so an operator who finds the guide first follows a superseded procedure — and its residue is what makes `smd status` contradict `smd psws status` (row 7) | operator/registration.md §5b | — |
| 11 | sigmond | "my heartbeat is landing on the board" should be answerable from the station | it is not. The only local verb, `smd admin heartbeat show`, reads the spool at `/var/lib/sigmond/heartbeat` — which hs-uploader's `heartbeat_sftp` transport drains on success, so on a **healthy** station it reports `no heartbeat ticks` and on a **broken** one it reports a backlog. The healthy case is indistinguishable from "never emitted". Verified live on b4 2026-08-23: `sigmond-heartbeat.timer` enabled + active, last fired 4 min earlier, spool directory empty, `smd admin heartbeat show` → `✗ no heartbeat ticks`. The operator page has to say "ask your fleet admin" | operator/day-2.md | — |
| 12 | sigmond | `⚠ N pinned process(es) overlap radiod cores` should mean something is wrong | both fleet stations show it permanently while producing good data (b4 22, dasi002 18 on 2026-08-23; real userspace processes — python recorders, uvicorn, ka9q-web, hs-uploader — not kernel threads, which `lib/sigmond/cpu.py::find_contending_processes` already excludes). A warning that is true on 100% of healthy hosts cannot flag the regression it exists to catch, and the operator has no read-only verb that lists *which* processes overlap (`bin/smd` line ~8609 prints the count only; the sample-listing variant in `cpu.py` is not the one `smd status` uses) | operator/day-2.md | — |
| 13 | sigmond | `smd admin rac status` inside a station VM should describe that station's remote access | on an appliance station it describes a *different* tunnel and then recommends a harmful action: both fleet VMs print `⚠ not configured on this host (/etc/sigmond/frpc.toml / wd-rac.service missing)` and `provision with: smd admin rac install`, while the station's real, healthy tunnel is `sigmond-rac-host.service` + `/etc/sigmond/frpc-host.toml` on the Proxmox host (both live 2026-08-23, 4/4 channels up on b4). Following the printed advice would stand up a second, redundant tunnel. The VM verbs have no way to detect that they are running inside an appliance VM and defer to the host | operator/remote-access.md §4 | — |
| 14 | sigmond-appliance | one read-only command on the host that answers “is my remote access healthy?” | there is none. `sigmond-setup` has `--rac-on`/`--rac-off`/`--rac-upgrade`/`--reconfigure` but no `--rac-status`, and `sigmond-setup --help` on a configured host just prints `already configured … run --reconfigure`. To confirm all four channels are up an operator must curl frpc's JSON admin API on `127.0.0.1:7500` and count occurrences of `"status":"running"` — the same trick the wizard uses internally. `systemctl is-active sigmond-rac-host` reports the process, not the channels | operator/remote-access.md §4 | — |
| 15 | sigmond | `smd --help` should not tell the operator to do the one thing `smd` rejects | its epilog labels the lifecycle group `Operator (mutating — needs sudo)` (`bin/smd`, the `argparse` epilog), but `smd` refuses a sudo-wrapped invocation outright — the top-of-`main` guard prints `don't run smd under sudo — it elevates itself when a verb needs root` whenever EUID is 0 with `SUDO_USER` set and `SIGMOND_ALLOW_SUDO` unset. `_need_root()` is what actually elevates, by re-exec'ing `sudo -- env SIGMOND_ALLOW_SUDO=1 smd <same argv>`. "needs sudo" is true of the verb and false of the operator, and the operator is who reads help text | operator/troubleshooting.md | — |
| 16 | sigmond | an operator should be able to read a failed client's log | neither operator account can. `smd admin log <client>` (journal mode) `execvp`s a plain `journalctl --follow` with no elevation (`lib/sigmond/log_cmd.py::follow_journal`), and neither `hamsci` nor `sigmond` is in `adm` or `systemd-journal` — both groups are empty on b4 (`getent group adm systemd-journal` → `adm:x:4:` / `systemd-journal:x:999:`, live 2026-08-23). The result on both fleet stations is `No journal files were opened due to insufficient permissions` and no log lines, which is indistinguishable from a service that logged nothing. The working paths are `systemctl status <unit>` and `smd admin log <client> --files` (proven live on dasi002: the `--files` form is what revealed mag-recorder's EX_CONFIG exit) | operator/troubleshooting.md | — |
| 17 | sigmond | "this station has no magnetometer" should be a state, not a permanent failure | on a station without an RM3100 `mag-recorder.service` stays `failed` forever: the shipped config keeps its template placeholders and the daemon exits `EX_CONFIG` (78) on every start — live DASI002 2026-08-23, `Active: failed (Result: exit-code) … status=78`, log `config still at template placeholders (station.psws_station_id=<YOUR_PSWS_STATION_ID>) … exiting EX_CONFIG`. `smd status` renders that identically to a station whose magnetometer genuinely broke, so the operator page has to teach the reader to tell the two apart by reading the log | operator/troubleshooting.md, operator/day-2.md | — |
| 18 | mag-recorder | a stuck sensor should not look like good data | when the RM3100 stops answering on I²C, the recorder keeps emitting one sample per second carrying the last good reading, so the unit stays `active`, `smd status` stays ✓, and the dashboard draws a flat, perfectly constant line that reads as data. Observed on AC0G/B4 from 2026-08-18 to 2026-08-21; three frozen days were packaged and uploaded before anyone looked at the trace. Nothing on the station — not `smd status`, not the heartbeat — flags a constant-valued sensor, and a replug additionally requires a `smd restart mag-recorder` because the reader does not reopen the device | operator/troubleshooting.md | — |
| 19 | sigmond | `smd watch mag` should be the operator's liveness check for the magnetometer | it reports `samples=  0 (0.0/s)  — no samples; is /dev/ttyMAG0 present?` on a station whose sensor is demonstrably alive. Live on b4 2026-08-23: `mag-recorder.service` active, `/dev/ttyMAG0 -> ttyACM3` present, and `/var/lib/mag-recorder/samples-2026-08-23.jsonl` growing with varying `x`/`y`/`z` and `rt` ≈ 31 — while `smd watch mag` showed zero samples for its whole 10 s window and printed a hint naming a device that was in fact there. The operator page has to send readers to `tail` the JSONL instead | operator/troubleshooting.md | — |
| 20 | hf-timestd | one number for how fast the timing client fills a disk | The two source docs disagree by ~2.7× and neither has been corrected: `resource_guardian.py:13-17`'s docstring budgets **~18 GB per channel per day** (raw ~14 GB + phase2 ~4 GB of "HDF5 data products"), while `hf-timestd/INSTALLATION.md` §Storage Requirements says **6.7 GB** and calls 9 channels on a 120 GB disk the absolute margin. The operator pages settle it with a **b4 measurement, 2026-08-23** (a complete UTC day of one channel, 15,073,610,352 B), but quote two different numbers on purpose: `day-2.md` quotes **≈16 GB/channel/day** (raw + the amortised `phase2` SQLite database), while `shopping-list.md`'s table row quotes **15 GB/channel/day raw-only** (b4's `phase2` is 99.9 % one cumulative database with 292-byte per-channel directories, not per-channel HDF5, so the code's phase2 half over-counts) | operator/day-2.md, hardware/shopping-list.md, operator/troubleshooting.md | — |
| 21 | sigmond-appliance | "the station's packages are held where they are on purpose" should be enforced, not merely believed | nothing on either fleet station holds them. `apt-mark showhold` is **empty** on b4 and dasi002 (live, 2026-08-23), there is no apt preferences pin and no unattended-upgrades policy in the image, yet radiod, ka9q-web, `wsprd`/`jt9`, `dumphfdl` and `mag-usb` are native binaries built or shipped against exactly the libraries now installed (`_install_radiod_native` in `bin/smd`; `docs/native-binaries.md`). A single `apt upgrade` — or a distro auto-upgrade nobody disabled — can move that ground with no warning, no `smd doctor` finding, and no verb that rebuilds against it. The operator page can only ask politely | operator/do-not-touch.md, operator/day-2.md | — |
| 22 | sigmond | `smd apply --help` should tell an operator what `apply` can move | it is four lines — `usage: smd apply [-h] [--dry-run]`, `--dry-run` undocumented, and the subcommand help is `reconcile services with current config` (live b4, 2026-08-23). What `cmd_apply` actually does is enable and start units, rewrite radiod's CPU-affinity drop-ins and the cpufreq governor on its cores, rewrite the `radiod@*.conf.d/` fragments, reconcile the RX888 FX3 firmware variant **and restart radiod when that firmware changed**, then restart the uploaders. "Reconcile services with current config" reads like a no-op to the person most likely to type it | operator/do-not-touch.md | — |
| 23 | sigmond | a station running a fork or superset checkout of ka9q-radio should say so, and `smd install ka9q-radio` should refuse to silently revert it | neither happens. b4's radio checkout is on branch `upgrade-2026.08.15` at `7fca458a` tracking a **fork** remote (`mjh` → `github.com/mijahauan/ka9q-radio`), carrying RX888 firmware/serial work upstream does not have (live, 2026-08-23; dasi002 is on the plain pin `cd44bbdd`, detached). `lib/sigmond/doctor.py` inspects ownership, git state, venv skew, exec mismatch and manifest drift but never which **remote** a checkout tracks, so `smd doctor` reports b4 clean; and `_install_radiod_native` re-pins to ka9q-python's compat commit with no prompt, exactly the revert `_rebuild_component` warns about in its own comment. The rebuilt station comes up all-ticks and the loss is invisible until someone runs `git log` in the right directory | operator/do-not-touch.md | — |
| 24 | mag-recorder + sigmond | a config warning should say **whose** config it is about | `mag-recorder`'s contract check emits `✗ station.psws_station_id is unset (need PSWS-issued S0xxxxx)` and `⚠ station.callsign is unset` (`mag_recorder/contract.py::_collect_issues`), and `smd status` prints them bare — the first *above* the status banner. Both name keys in `/etc/mag-recorder/mag-recorder-config.toml` (`mag_recorder/config.py:22`, `DEFAULT_CONFIG_PATH`; the only file in `/etc/mag-recorder/` on dasi002), but `[station] callsign` is also a key in `/etc/sigmond/site-profile.toml`, so on dasi002 the tool says the callsign is unset while `site-profile.toml` reads `callsign = "DASI002"` (live, 2026-08-23). An operator reading the operator guide is told site-profile.toml is where identity lives, and cannot tell these lines belong to a different file. Prefixing the client name, or the config path, would end it | operator/day-2.md §1 (walk-through F2, F3) | — |
| 25 | sigmond | "is this client off on purpose?" should be answerable on the station | it is not. `smd component list`'s LIFECYCLE column distinguishes `enabled, running` / `enabled, stopped` / `configured` / `binary, on PATH` / `binary, missing` / `library` (live b4 + dasi002, 2026-08-23), which is enough to see *that* dasi002's `wspr-recorder` and `psk-recorder` are installed-but-not-enabled — but nothing anywhere records *who* decided that or *why*, and `smd status` simply omits them, which is the exact shape README.md tells the operator to escalate ("a client missing … ask"). A `reason =` on a disabled client in `topology.toml`, surfaced by `smd status` as one line, would turn a phone call into a read | operator/day-2.md §1, operator/README.md (walk-through F1, F13) | — |
| 26 | sigmond | `smd doctor`'s repairable count should be legible next to its finding list | it prints `N finding(s) repairable with: smd doctor --fix` where N counts only findings with `fixable=True`, and `fixable=True` is set in exactly one place — the `ownership` finding (`bin/smd` ~4504, counted at ~4786). Every `untracked`, `detached` and `dirty` line is unfixable by design. So dasi002 showed nine finding lines and `3 finding(s) repairable` (live, 2026-08-23) with nothing on screen saying which three, while day-2's b4 sample happens to show three lines and "3 finding(s)", teaching the wrong mapping. Tagging the count (`3 ownership finding(s) …`) or the lines would fix it; `[--fix repairs this]` is already rendered per-line by `doctor.py:557` but only for some | operator/day-2.md §4 (walk-through F9) | — |
| 27 | sigmond | `smd watch uploads` should not fail closed with an undocumented fourth outcome | on a station whose recorders are not enabled it returns **immediately** with `✗ uploads-watch: no active uploader on this host (wspr-uploader.service, wspr-recorder@*, psk-recorder@*, wd-upload-hs@* all inactive).` (live dasi002, 2026-08-23) — while both operator pages that use the command tell the reader to press Ctrl-C to stop it, so an operator cannot tell whether it ran. It also names four systemd units that appear in no operator page, one of them (`wspr-uploader.service`) a retired name. This is the documented confirmation step for three of the four data paths. Saying "uploads are disabled on this station — see `smd config uploads status`" instead would answer the operator's actual question | operator/registration.md §2 §6 §8, operator/troubleshooting.md (walk-through F16) | — |
| 28 | sigmond | *(merged into row 31)* | *(merged into row 31)* | — | — |
| 29 | sigmond | one answer to "are my uploads working?" inside one weekly check | two commands the operator pages both send them to disagree on the same station. `smd component list`'s upload-readiness block prints `✓ ready: wsprnet.org, wsprdaemon.org, PSKReporter` on dasi002, while `smd config uploads status` on the same station prints `⚠ uploads: DISABLED BY POLICY` (live, 2026-08-23). Both are correct — `lib/sigmond/upload_creds.py`'s `ready` field is documented as "are the required credentials/identity present?" and the three spot networks need no credentials at all, so they read ready on every station ever — but nothing in the readiness block knows the `[uploads]` policy switch exists, so the operator's own weekly evidence contains a flat "ready" against a flat "disabled". One `(uploads disabled by policy)` suffix on the readiness header would end it | operator/day-2.md §1 (walk-through pass 2, F5) | — |
| 30 | mag-recorder | the PSWS key path a client checks should be the path the station actually uses | `mag_recorder/config.py:169` still defaults `uploader.ssh_key_file` to the **legacy shared** `/etc/hs-uploader/keys/id_ed25519`, while today's station key is `/etc/hs-uploader/keys/id_ed25519_host` — what `smd psws enroll` creates (`lib/sigmond/psws.py`, `STATION_KEY`) and what `uploader_manifest.py` writes into `pipelines.toml`. sigmond's own `upload_creds.py:34-41` already accepts either name and says why; mag-recorder's `contract.py:267-272` does not, so it reports the legacy path missing and appends *"ssh-keygen + register key on PSWS portal first"* — advice that contradicts registration.md's `smd psws enroll` flow. Live 2026-08-23: dasi002 holds only `id_ed25519_host` and prints the ⚠; b4 holds both and does not. A one-line default change, or the same either-name check sigmond already has | operator/day-2.md §1, operator/registration.md §5b (walk-through pass 2, F2/F6) | — |
| 31 | sigmond | operator-facing output should not recommend commands the operator guide forbids | three do, and none of them knows it. `smd component list` ends with `run: smd component update` (do-not-touch: forbidden — it pulls every repo then runs a full apply); its upload-readiness `↳ fix:` lines print `smd config edit hf-timestd` and `sudo bash /opt/git/sigmond/hf-timestd/scripts/setup-psws-keys.sh` (the superseded per-recorder key procedure); `smd status`'s PSWS block prints `finish: smd config hf-timestd edit` (row 28). All are written for whoever installs a station and all reach whoever is reading the screen. The operator pages now carry a ⛔ note beside each place the output is shown, which is a documentation patch over a message-audience problem | operator/day-2.md §1, operator/registration.md §5b (walk-through pass 2, F6) | — |
| 32 | sigmond | `smd component list`'s LIFECYCLE should distinguish `failed` from `stopped` | it cannot: the cell is computed from `systemctl is-active` / `is-enabled` alone (`bin/smd`, the lifecycle branch), so a unit that crashed is "not active, still enabled" and prints as **`enabled, stopped`**. Live 2026-08-23, dasi002's `mag-recorder` reads `failed` in `smd status` and in `systemctl status`, and `enabled, stopped` here, on the same morning. The one alarming word on the whole weekly check is rendered by the sibling command as a reassuring one; `systemctl is-failed` is one more call | operator/day-2.md §1 (walk-through pass 2, F4) | — |
| 33 | hf-timestd | a scientist budgeting a capture that may outlive GPS lock needs a **numeric** holdover drift rate | there is none published. `docs/METROLOGY.md` §4.5 ranks T1 ("coasting on the GPSDO") with `const offset at snapshot + 0 drift` and its prose calls it "a degraded holdover, not a steady operating point" — but *zero* drift is an idealisation of an A1 timebase that is ppb-stable, not perfect, and no doc, config comment or issue in the repo gives a µs/hour figure to budget against (grepped `hf-timestd/docs/*.md`, 2026-08-23: the only `1.44` hits are the ionospheric `foE` coefficient). The authority manager already persists per-cycle records to `/var/lib/timestd/authority_history.db`, so the number is measurable from data the station already keeps; publishing it (or the measurement method) would let a client decide whether an unanchored hour is usable | scientist/station-capabilities.md §Timing, scientist/data-and-timing.md (Phase 2) | — |
| 34 | ka9q-radio | `docs/SDR/rx888.md` should not deny a feature the driver implements and runs by default | its §gain section says "There is no front end AGC in hardware or software (yet) so this should be set with some care", while `src/rx888.c` starts an `agc_rx888` thread that wakes every second (`AGC_INTERVAL = 1`, l.52) and rewrites the AD8370 VGA gain whenever the input level leaves `[agc-low-threshold, agc-high-threshold]` (l.575, l.660), with `frontend->rf_agc = true` as the default unless a `gain`/`rfatten` is configured (l.238, l.298, l.317). Neither is configured on b4, and radiod reports `[99] rf agc enabled`, `[98] rf gain +16.5 dB` live (2026-08-23). A scientist reading the doc concludes their channel's absolute amplitude is stable when the front end is in fact hunting; the two config keys that turn it off are undocumented there too | scientist/station-capabilities.md §AGC and gain, hardware/character.md (Phase 2) | — |
| 35 | hf-timestd | `METROLOGY.md` §4.3's justification for treating `(GPS_TIME, RTP_TIMESNAP)` as a sample-precise pair should match ka9q-radio's source | it does not. §4.3 says the two "are both derived from `input_sample_index / decimation` — they are in the same counter space. No pipeline offset correction is needed." In `encode_radio_status()` the two are written from different clocks at different instants: `GPS_TIME` is `gps_time_ns()` evaluated as the status packet is built (`ka9q-radio/src/radio_status.c:718-719`), and `gps_time_ns()` is `clock_gettime(CLOCK_TAI)` offset to the GPS epoch — the **host system clock** (`ka9q-radio/src/misc.c:546-563`) — while `RTP_TIMESNAP` is `chan->output.rtp.timestamp` (`radio_status.c:859`), "the next RTP timestamp to be sent" (`src/monitor-display.c:886`), advanced by the frame count each time a block is emitted (`src/audio.c:49-51`, l.177-179) and therefore quantised to the 20 ms block grid plus that emission's lateness. §4.3's own "Prerequisites for authoritative" paragraph already hedges on A-level and RTP_TIMESNAP freshness, so the fix is the counter-space sentence, not the conclusion. A scientist reading §4.3 will attribute a sub-block offset to physics | hardware/character.md §Timing-chain caveats, scientist/data-and-timing.md (Phase 2) | — |
| 36 | hf-timestd | the two T6 documents should agree on whether the measured BPSK chain delay is applied to timestamps | they contradict each other. `docs/STATION_SETUP_GUIDE.md` §BPSK-PPS (l.227, l.231) says "After 10 consecutive valid PPS edges, the measured chain delay is applied to all other channels' RTP-to-UTC mapping automatically" and calls it "typically a few milliseconds at 24 kHz sample rate". `docs/ARCHITECTURE-FIRST-PRINCIPLES.md` §"Under the T6 anchor inversion" says the opposite for today's code: `anchor_utc_ns = named_integer_second + delay_budget_ns` with `delay_budget_ns` "a bounded (±1 ms hard validation bound), configured constant — not a per-lock fitted quantity", measured `chain_delay` kept "only as a reported diagnostic … never applied as a correction", because "every historical `chain_delay` fit (32–106 ms) violated the microsecond-class analog-path definition … by three to four orders of magnitude". A scientist reading the setup guide will assume a correction was subtracted that was not. Either the setup guide needs the inversion, or it needs a pointer to the page that supersedes it | hardware/character.md §The TS-1, scientist/data-and-timing.md (Phase 2) | — |
| 37 | ka9q-python | `docs/RTP_TIMING_SUPPORT.md` should not promise more precision than the pair can carry (the mirror of row 35, on the client side) | it says the two fields "let you convert any RTP timestamp in a packet header into a **sample-accurate** Unix wallclock time" (l.5) and defines `RTP_TIMESNAP` as "the RTP timestamp value **at that exact GPS time**" (l.24). In `ka9q-radio` they are not co-sampled: `GPS_TIME` is `gps_time_ns()` (the host `CLOCK_TAI`) read as the status packet is built (`src/radio_status.c:718-719`, `src/misc.c:546-563`), while `RTP_TIMESNAP` is the channel's next-to-send RTP timestamp (`src/radio_status.c:859`, `src/monitor-display.c:886`), advanced per emitted block (`src/audio.c:49-51`) — so the pair is block-grid plus emission lateness. The library's own 3.19.0 release notes measure the consequence and say so: the pair "jitters ~0.45 s and occasionally tears between ~450 ms snapshots" (`CHANGELOG.md` l.238-239), which is why per-update step detection was removed. The doc also still names the deprecated `rtp_to_wallclock()` rather than `rtp_to_utc()` (`ka9q/rtp_recorder.py:128`) | hardware/character.md §Timing-chain caveats, scientist/data-and-timing.md (Phase 2) | — |
| 38 | ka9q-python | `pip install ka9q-python` should give a scientist the library the stations actually run | PyPI is three releases behind the repo. Live on DASI002, 2026-08-23: `pip index versions ka9q-python` → `Available versions: 3.22.0, 3.20.0, 3.19.0, …` / `LATEST: 3.22.0`, and a fresh `pip install ka9q-python` resolved to **3.22.0**, while `/home/mjh/hamsci/repos/ka9q-python/pyproject.toml` (and the fleet checkouts at `/opt/git/sigmond/ka9q-python`) are on **3.25.2**. The three missing releases are not cosmetic: 3.25.1 fixed `create_channel()` sending `DEMOD_TYPE = FM` for every preset outside a five-name allowlist — on 3.22.0 a `wfm`, `sam`, `ame`, `dsb`, `cwu`, `cwl`, `wspr`, `nam`, `amsq` or `spectrum` channel is created, verifies, and **never emits RTP**; 3.25.0 added `FILTER_DROPS` decoding to `decode_status_dict()` (the only continuous path to radiod's own loss counter) and `ChannelInfo.filter_drops`; 3.25.2 added `poll_channel(allow_idle=True)` (#6). The scientist recipe therefore has to warn readers off PyPI for any preset outside `iq/usb/lsb/cw/am` and send them to `pip install -e /opt/git/sigmond/ka9q-python` instead — publishing 3.25.2 to PyPI would remove the caveat | scientist/capture-quickstart.md §Option B | — |
| 39 | ka9q-radio | radiod's status should not report an encoding its own wire contradicts | it reports `encoding = 2` (S16BE) for IQ channels that are demonstrably sending F32LE. Measured live on DASI002, 2026-08-23, with a fresh `ensure_channel(encoding=F32LE, preset="iq", sample_rate=12000)`: the returned `ChannelInfo.encoding` was `2`, while payload-bytes ÷ RTP-ticks ÷ 2 components over 6000 packets measured **4.00 bytes/component**, and 5,760,000 bytes ÷ 8 = 720,000 samples = exactly 60.0 s at 12 kHz — i.e. the grant took effect and only the *report* is wrong. This is the inverse of the "grant silently lost" failure `ka9q-python/ka9q/control.py:946-948` already tracks (HamSCI/ka9q-python#3), and it is more dangerous because `RadiodStream` decodes with the **reported** value (`getattr(self.channel, 'encoding', 0)`): believing the report parses 2 bytes where 4 were sent and yields a plausible, well-formed array at twice the sample count with clean completeness and zero gaps. `event-recorder` found it independently on B4 (`Costas-array/src/event_recorder/channel.py::_assert_encoding`, "radiod's status ALWAYS reports encoding=2 for IQ channels") and works around it by probing the wire; every client now has to. Either radiod should report what it sends, or ka9q-python should stop surfacing the field as authoritative | scientist/capture-quickstart.md §Option B, hardware/character.md §The encoding you asked for is a second command | — |
| 40 | ka9q-python | `RTPRecorder.get_metrics()['packets_received']` should count the caller's channel, not the host's port | it counts every datagram arriving on the bound port before the SSRC filter runs (`ka9q/rtp_recorder.py::_receive_loop` increments `metrics.packets_received` immediately after `recvfrom`, while `_validate_packet` — which drops wrong-SSRC packets — is called afterwards). Because the recorder binds `0.0.0.0:<channel.port>` and every radiod channel on a station publishes to port 5004, the counter is a measure of the whole host. Live DASI002, 2026-08-23: a 60 s single-SSRC capture that wrote **6,000** packets reported `packets_received: 70,400` — an 11.7× overcount contributed by the station's eight other channels. `packets_dropped`, `sequence_errors` and `timestamp_jumps` are correctly per-SSRC (they are computed inside `_validate_packet`), so one field in the same dict has different semantics from its neighbours, with nothing saying so. A client using `packets_received` as its capture's denominator computes a completeness figure that is silently wrong | scientist/capture-quickstart.md §The sidecar | — |
