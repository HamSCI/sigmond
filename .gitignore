__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
build/
dist/
# Claude Code session data
.claude/
# wsprd runtime artifacts (written by wd-decode when run from this directory)
ALL_WSPR.TXT
hashtable.txt
wspr_spots.txt
wspr_timer.out
wspr_wisdom.dat
jt9_wisdom.dat
.wsprd_result
*.c2
# Dependency repos managed at this level (not committed)
ka9q-radio/
ka9q-web/
onion/
# Generated fresh on every deploy from `smd fleet roster --json` — a
# checked-in copy would go stale, and a stale roster means a station is
# never asked about instead of going red (server/heartbeat/deploy-wd30.sh).
server/heartbeat/roster.json
